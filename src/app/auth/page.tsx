import { RoutePlaceholder } from "@/components/ui/route-placeholder";

export default function AuthPage() {
  return (
    <RoutePlaceholder
      title="Sign in"
      description="Supabase Auth (email) will be wired here."
    />
  );
}
